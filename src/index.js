import React from 'react';
import ReactDOM from 'react-dom/client';
import ArabicLearningApp from './ArabicLearningApp';
import './index.css';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <ArabicLearningApp />
  </React.StrictMode>
); 